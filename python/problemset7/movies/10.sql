SELECT name FROM people WHERE id IN (SELECT DISTINCT(person_id) FROM movies,ratings,directors,people WHERE movies.id=ratings.movie_id AND movies.id=directors.movie_id AND directors.person_id=people.id AND rating>=9.0);